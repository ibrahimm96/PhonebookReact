// TabTwoScreen.js

import React from 'react';
import { View, Text, ActivityIndicator, FlatList } from 'react-native';

const TabTwoScreen = () => {
  //const { data, isLoading } = useGetData(); // Call the useGetData hook

  return (
    <View>
      <Text>TAB TWO</Text>
    </View>
  );
};

export default TabTwoScreen;
